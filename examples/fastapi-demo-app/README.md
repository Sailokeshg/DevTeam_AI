# FastAPI Demo App

Small local demo repository used by DevTeam AI tests and example runs.

## Curated Phase 15 Tasks

- Add health endpoint
- Add input validation
- Add JWT auth skeleton
- Fix failing ping test

Run tests from this directory:

```bash
pytest
```

Run the Phase 15 evaluation harness from the repository root:

```bash
python scripts/evaluate_demo_tasks.py
```
